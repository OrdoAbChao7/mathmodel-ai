# Formal competition-max fixture

This deterministic fixture is a local integration case for the full CUMCM
`competition_max` contract. `solve.py` materializes the evidence registries,
human-review records, experiment provenance, freeze manifest, paper package,
and max-rigor records. The normal CLI still compiles the paper and recomputes
G0--G9; the fixture is not an award-quality claim.
